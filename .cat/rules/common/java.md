---
paths:
  - "*.java"
  - "pom.xml"
  - "**/pom.xml"
  - "build.sh"
  - "**/build.sh"
---
# Java Convention Loading Index

When changing Maven configuration, build scripts, dependencies, or build/lint/test command policy:
Lazy load `../include/java/build.md`.

When editing Java source style, imports, naming, formatting, control flow, comments, regex, Unicode, or string
comparison patterns:
Lazy load `../include/java/style.md`.

When writing or editing Javadoc, Javadoc links/imports, or Java API documentation:
Lazy load `../include/java/documentation.md`.

When handling strings, text blocks, external string checks, parameter validation, return-value validation, or
requirements assertions:
Lazy load `../include/java/strings-validation.md`.

When designing classes, interfaces, scopes, constructors, builders, method visibility, entrypoint classes, or
business-logic boundaries:
Lazy load `../include/java/class-design.md`.

When changing warning suppression, environment access, exception types, checked-exception wrapping, `main()` error
handling, or logging:
Lazy load `../include/java/runtime.md`.

When writing or changing Java tests, assertions, exception tests, test documentation, testability seams, clocks, or
thread-related test behavior:
Lazy load `../include/java/testing.md`.

When editing `module-info.java`, module exports/opens, module names, `build.sh`, module layout, or Java project layout:
Lazy load `../include/java/modules.md`.
