---
description: "MANDATORY: Use instead of `git filter-branch` - safer git-filter-repo with recovery"
model: sonnet
user-invocable: false
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/load-skill" "${CLAUDE_PLUGIN_ROOT}" git-rewrite-history "${CLAUDE_PROJECT_DIR}" "$0"`
