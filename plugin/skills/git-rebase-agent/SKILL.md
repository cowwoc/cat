---
description: "MANDATORY: Use instead of `git rebase` - provides automatic backup and conflict recovery"
user-invocable: false
argument-hint: "<cat_agent_id>"
effort: medium
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/get-skill" git-rebase-agent "$0"`
