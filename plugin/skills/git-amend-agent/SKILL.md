---
description: "MANDATORY: Use instead of `git commit --amend` - verifies HEAD and push status first"
model: haiku
user-invocable: false
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/skill-loader" git-amend-agent "$0"`
