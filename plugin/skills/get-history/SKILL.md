---
description: Use when you need to examine past conversation, session logs, or raw chat history
model: haiku
user-invocable: false
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/load-skill" "${CLAUDE_PLUGIN_ROOT}" get-history "${CLAUDE_SESSION_ID}" "${CLAUDE_PROJECT_DIR}"`
