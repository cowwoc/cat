---
description: "MANDATORY: Use instead of `git rebase` - provides automatic backup and conflict recovery"
model: sonnet
user-invocable: false
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/load-skill" "${CLAUDE_PLUGIN_ROOT}" git-rebase "${CLAUDE_SESSION_ID}" "${CLAUDE_PROJECT_DIR}"`
