---
description: "MANDATORY: Use instead of `git commit --amend` - verifies HEAD and push status first"
model: haiku
user-invocable: false
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/load-skill" "${CLAUDE_PLUGIN_ROOT}" git-amend "${CLAUDE_PROJECT_DIR}" "$0"`
