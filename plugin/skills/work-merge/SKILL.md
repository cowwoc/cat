---
description: Internal merge phase (invoked by /cat:work) - squashes commits, merges to base branch, cleans up
user-invocable: false
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/load-skill" "${CLAUDE_PLUGIN_ROOT}" work-merge "${CLAUDE_SESSION_ID}" "${CLAUDE_PROJECT_DIR}"`
