---
description: Use when an issue is too large or needs breaking down - split into smaller, manageable issues
user-invocable: false
---

!`"${CLAUDE_PLUGIN_ROOT}/client/bin/load-skill" "${CLAUDE_PLUGIN_ROOT}" decompose-issue "${CLAUDE_SESSION_ID}" "${CLAUDE_PROJECT_DIR}"`
