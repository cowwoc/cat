---
category: sequence
---
## Turn 1

I need to rename `UserService` to `AccountService` across the codebase. I haven't searched for any occurrences yet. What is the very first action I must take before making any file edits?

## Assertions

1. agent specifies that a grep scan for all occurrences of UserService must happen before any edits
2. response does not suggest starting edits without scanning first
