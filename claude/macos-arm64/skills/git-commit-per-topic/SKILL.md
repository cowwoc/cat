---
description: Use when the user asks to rewrite history so each commit introduces one working feature or bug fix, organize commits by topic, fold fixes into an earlier feature commit, or split a composite commit or class into independently buildable steps.
allowed-tools: [Bash]
---

Use `${CAT_PLUGIN_ROOT}/client/bin/git-topic-replay` for the installed CAT plugin.

# Shared First Use

## Design Goals

- Ensure an agent reads and follows a selected skill's complete instructions once in each conversation context.

## Guidance

If you have not already read this skill's `first-use.md` in the current
conversation context, read it now and follow it exactly. Otherwise, reuse its guidance.
Read `first-use.md` again after the context resets or is compacted.

