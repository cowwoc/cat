# CAT Lazy Rule Dependencies

Before lazy loading a listed rule, first lazy load its listed prerequisites.

When lazy loading `../include/java/javadoc.md`, first lazy load `../include/common/write-clearly.md` and `../include/documentation/general.md`.
