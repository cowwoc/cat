# CAT Path-Scoped Rules

Load `rules/include/common/prompt-file-dependencies.md` only when an accessed file matches [**/rules/**/*.md].
Load `rules/include/common/prompt-file-references.md` only when an accessed file matches [**/skills/**/*.md, **/agents/**/*.md, **/rules/**/*.md].
